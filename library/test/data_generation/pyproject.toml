[project]
name = "data_generation"
dependencies = ["numpy>=2.3.4", "matplotlib>=3.10.7"]
requires-python = ">=3.11"
readme = "README.md"

[tool.pdm]
distribution = false

[tool.pdm.scripts]
generate_noisy_data = "python scripts/generate_noisy_data.py"
